{
  "scripts": {
    "cleanup": "rm -rf .cache dist && mkdir dist && cp -R data dist",
    "dev": "parcel index.html",
    "build": "parcel build index.html",
    "watch": "parcel watch index.html"
  },
  "devDependencies": {
    "cssnano": "^4.1.10"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "parcel-bundler": "^1.12.4",
    "vue": "^2.6.11",
    "vuelidate": "^0.7.5"
  }
}
